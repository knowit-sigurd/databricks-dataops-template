resources:
  pipelines:
    gold_pipeline:
      name: "[${var.target_schema}] gold_pipeline"
      catalog: ${var.catalog}
      target: ${var.target_schema}
      serverless: true
      configuration:
        catalog: ${var.catalog}
        target_schema: ${var.target_schema}
      libraries:
        - file:
            path: ../src/data_product/pipelines/gold_pipeline.py
      permissions:
        - level: CAN_VIEW
          group_name: data-operators

      environment:
        dependencies:
          - --editable ${workspace.file_path}
