resources:
  pipelines:
    gold_pipeline:
      name: "[${var.catalog}] gold_pipeline"
      catalog: ${var.catalog}
      target: ${var.target_schema}
      serverless: true
      libraries:
        - file:
            path: ../src/data_product/pipelines/gold_pipeline.py
      environment:
        dependencies:
          - --editable ${workspace.file_path}
